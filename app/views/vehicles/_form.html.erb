<%= form_with(model: vehicle, local: true) do |form| %>
  <% if vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
      <% vehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :vehicle_name %>
  </div>

  <div class="field">
    <%= form.label :launch_date %>
    <%= form.date_select :launch_date, id: :vehicle_launch_date %>
  </div>

  <div class="field">
    <%= form.label :brand_id %>
    <%= form.collection_select :brand_id, Brand.all, :id, :name, {:include_blank => true}, {:id => 'vehicle_brand_id'} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
